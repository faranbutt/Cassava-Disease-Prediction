# working/Cassava-Disease-Detection/configs/config.yaml

defaults:
  - model: model1
  - train: train
  - data: data    # ← Fixed!
  - _self_

model_name: "model1"
debug: true       # ← Set to true for testing
debug_samples: 100
run_full: false

artifacts:
  save_ckpt: false      # default: no
  save_onnx: true       # always yes

predict:
  model_path: "models/model1/model_best.ckpt"
  image_path: "data/example.jpg"